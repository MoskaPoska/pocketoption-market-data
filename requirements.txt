# Core async framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0       # ASGI server — pulls uvloop on Linux automatically
uvloop>=0.19.0                  # Fast event loop (Linux/macOS only, ignored on Windows)

# WebSocket collector
aiohttp[speedups]>=3.9.5        # speedups = aiodns + brotli (works on Linux)

# Redis Pub/Sub
redis>=5.0.4

# Configuration
pydantic-settings>=2.2.0

# Binary frame decoding (PocketOption updateStream)
msgpack>=1.0.8

# TLS fingerprinting for Cloudflare bypass
curl_cffi>=0.7.1
